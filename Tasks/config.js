const taskDir = './Tasks';

module.exports = [
	`${taskDir}/pages`,
	`${taskDir}/styles`,
	`${taskDir}/scripts`,
	`${taskDir}/static`,
	`${taskDir}/watch`,
	`${taskDir}/serve`,
	`${taskDir}/clean`,
	`${taskDir}/images`,
	`${taskDir}/favicon`
];
